#include <stdlib.h>
#include <time.h>
/**main-prints Hello,world
*
*Return:(0)
*/


int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	(alphabet) = n % alphabet;
        if      (alphabet > n)
        {
                ("Alphabet of \n is \n is alphabet in \n",n,alphabet);
        }
	return (0);
}
