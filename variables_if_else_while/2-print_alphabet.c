#include <stdlib.h>
#include <stdio.h>
#include <time.h>
/**main-prints Hello,world
*
*Return:(0)



int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	alphabet = n % alphabet;
        if      (alphabet > n)
        {
             printf ("Alphabet of \n is \n is alphabet in \n"(alphabet));
        }
	return (0);
}*/
int main(void)
{
	putchar('a' + 0 && printf("abcdefghijklmnopqrstuvwxyz"));
	putchar('\n');
	return 0;
}
